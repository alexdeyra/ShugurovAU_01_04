<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shugurovay_01_04">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/sber"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_sber_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar">
        <activity
            android:name=".SummaryActivity"
            android:exported="false" />
        <activity
            android:name=".BankActivity"
            android:exported="true"> <!-- Главная активность -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CreditCalculatorActivity"
            android:exported="false" />
    </application>

</manifest>